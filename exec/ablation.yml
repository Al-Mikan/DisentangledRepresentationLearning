train_mode: [mae, flow, gated] # mae / flow / gated
adversarial: ["off","kl", "dann"] # True: GAN方式 / False: 通常正則化
flow_preprocessing: ["normal", "centered"] # normal / centered
pooling: [true, false]
validation_split: [true, false]
use_augmentation: [true, false]
